[project]
name = "openviking-client"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "knowledge-model",
    "httpx>=0.27",
]

[tool.uv.sources]
knowledge-model = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/openviking_client"]
